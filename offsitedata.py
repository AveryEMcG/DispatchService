
"""OffsiteData
This module is a storage point for data assumed to be located somewhere out of scope - such as 
on external servers. Things are defined locally here in an arbitrary format so that they can be tested. in the code.

Things can and should be changed depending on the actual structure of the data.
"""

#programs associate a given dispatch with output types
program = {
    1:[("Email","example1@example.com"),("Email","example2@example.com")], 
    2:[("Email","example1@example.com"),("Email","example2@example.com")]
    }
